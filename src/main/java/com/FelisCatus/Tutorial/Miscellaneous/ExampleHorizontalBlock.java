package com.FelisCatus.Tutorial.Miscellaneous;

/**************************************
            =====注意！=====
本文件为测试用文件，代码可能不会正常运行!
**************************************/



/*
import net.minecraft.block.Block;
import net.minecraft.block.BlockState;
import net.minecraft.state.DirectionProperty;
import net.minecraft.state.properties.BlockStateProperties;
import net.minecraft.util.Mirror;


public class ExampleHorizontalBlock extends Block {
    public static final DirectionProperty H_FACE = BlockStateProperties.HORIZONTAL_FACING;
    public ExampleHorizontalBlock(Properties properties) {
        super(properties);
    }

    @SuppressWarnings("deprecation")
    @Override
    public BlockState mirror(BlockState state, Mirror mirrorIn)
    {
        return state.rotate(mirrorIn.toRotation(state.get(H_FACE)));
    }
    
}

*/